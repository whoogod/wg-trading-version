# wg-trading — version manifest

This repo hosts the update manifest for the **WG Trading** FiveM resource.
On boot, the script fetches `version.json` and prints a console notice if a
newer version is available. Nothing else is sent or received.

Purchase & updates: <your Tebex store link>
